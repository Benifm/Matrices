<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matrices">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Choose" />
        <activity android:name=".Determinant2" />
        <activity android:name=".Determinant3" />
        <activity android:name=".Determinant4" />
        <activity android:name=".Inverse2" />
        <activity android:name=".Inverse3" />
        <activity android:name=".Eigen2" />
        <activity android:name=".EigenV2"></activity>
    </application>

</manifest>